{
  "name": "unicode-property-escapes-tests",
  "private": true,
  "scripts": {
    "build": "rm -rf -- output/ && mkdir output/ && node build.js",
    "test": "npm run build"
  },
  "devDependencies": {
    "jsesc": "^2.5.0",
    "lodash.template": "^4.4.0",
    "regenerate": "^1.3.2",
    "regenerate-unicode-properties": "^5.0.1",
    "unicode-9.0.0": "^0.7.2",
    "unicode-property-aliases": "^1.1.1",
    "unicode-property-value-aliases": "^1.2.2"
  }
}
