{
	"name": "unicode-property-escapes-tests",
	"private": true,
	"scripts": {
		"clean": "rm -rf output && mkdir -p output/generated",
		"build": "npm run clean && node build.js && cp -r basic-tests/. output",
		"test": "node test.js"
	},
	"devDependencies": {
		"jsesc": "^2.5.2",
		"lodash.template": "^4.5.0",
		"regenerate": "^1.4.0",
		"regenerate-unicode-properties": "^8.2.0",
		"unicode-property-aliases-ecmascript": "^1.1.0",
		"unicode-property-value-aliases-ecmascript": "^1.1.0"
	}
}
